[template]
description  = "Greeter service + client in one program — a real gRPC round trip over loopback"
post_message = "cd into the project and `mcpp run`. The generated stubs in gen/ are protoc output for proto/helloworld.proto; the header comment in mcpp.toml shows how to regenerate them after you edit the .proto."
